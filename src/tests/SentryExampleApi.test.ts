import { createMocks } from 'node-mocks-http';
import handler from '../app/api/sentry-example-api/route';

describe('Sentry Example API', () => {
  it('POST: reports an error', async () => {
    // TODO: Mock Sentry and test error reporting
  });
}); 